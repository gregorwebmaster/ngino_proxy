server {
    listen          80;
    server_name     _;
    root            ${ROOT_DIR};
    index           index.php;

    location / {
        try_files ${DOL}uri ${DOL}uri/ /index.php?${DOL}args;
    }

    location ~ \.php$ {
        
        fastcgi_pass      ${PHP_BACKEND}:${PHP_PORT};
        fastcgi_index     index.php;
        include           fastcgi.conf;
    }
}